.env.local
.env
# All env files hold secrets — ignore every variant, keep only examples
.env.*
!.env.example
node_modules/
.next/
out/
.vercel/
*.log
.vercel

# Secret-bearing deploy/config files (gcloud env.yaml, etc.) — never commit
env.yaml
*.env.yaml
secrets.yaml

# Local scratch / one-off scripts and exports
scratch/
gist-export/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Editor + build artifacts
.vscode/
tsconfig.tsbuildinfo
